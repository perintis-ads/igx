{
  "scripts": {
    "dev": "wrangler dev src/index.tsx --local-protocol https",
    "local": "wrangler dev src/index.tsx  --local --local-protocol https --persist",
    "deploy:staging": "wrangler publish src/index.tsx --env staging",
    "deploy:prod": "wrangler publish src/index.tsx --env production"
  },
  "dependencies": {
    "hono": "^3.0.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20221111.1",
    "wrangler": "^2.7.1"
  }
}
